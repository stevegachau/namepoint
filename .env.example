# Optional. A free key from https://thegraph.com/studio lets the app query the
# ENS subgraph, which filters expired names and reverse records server-side.
# It is a rate-limit identifier, not a secret, and ships in the client bundle.
# Without it the app falls back to ENSNode, which needs no key.
VITE_ENS_SUBGRAPH_KEY=
