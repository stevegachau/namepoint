/**
 * Where the source lives. Kept in one place because the footer and the FAQ both
 * point at it, and the FAQ tells the reader the code is public — a claim that
 * needs a working link next to it.
 */
export const REPO_URL = "https://github.com/stevegachau/namepoint";
